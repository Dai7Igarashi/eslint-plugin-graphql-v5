# eslint-plugin-sample-v5

```console
$ npm install --save-dev ...

$ mkdir packages/eslint-plugin-graphql-v5
$ cd packages/eslint-plugin-graphql-v5 && npm init -y
$ npm install -w packages/eslint-plugin-graphql-v5 @graphql-eslint/eslint-plugin
$ npm install -w packages/eslint-plugin-graphql-v5 --save-dev graphql
```
